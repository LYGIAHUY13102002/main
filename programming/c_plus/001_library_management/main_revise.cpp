#include <iostream>
#include <unordered_map>    // hash-table data structure for lock-value mapping
#include <map>              // binary tree data structure (dynamic array)
#include <vector>           // vector data structure -> 
#include <string>

// Define data structure to store book information including ...
struct Book
{
    std::string isbn;
    std::string title;
    std::string author;
    int year;
    std::string genre;
    std::string location;
};


